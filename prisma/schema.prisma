datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

// Auth models
model User {
  id                  String               @id @default(uuid())
  email               String               @unique
  password            String
  role                Role                 @default(USER)
  sessions            Session[]
  emailVerifiedAt     DateTime?
  verificationTokens  VerificationToken[]
  passwordResetTokens PasswordResetToken[]
  loginAttempts       LoginAttempt[]
  orgMemberships      OrgMember[]
  technician          Technician?
  workNotes           WorkNote[]
  createdAt           DateTime             @default(now())
  updatedAt           DateTime             @updatedAt
}

model Session {
  id        String   @id @default(uuid())
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  userId    String
  token     String
  valid     Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([userId])
}

model VerificationToken {
  id        String    @id @default(uuid())
  user      User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  userId    String
  tokenHash String    @unique
  expiresAt DateTime
  usedAt    DateTime?
  createdAt DateTime  @default(now())

  @@index([userId])
  @@index([expiresAt])
}

model PasswordResetToken {
  id        String    @id @default(uuid())
  user      User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  userId    String
  tokenHash String    @unique
  expiresAt DateTime
  usedAt    DateTime?
  createdAt DateTime  @default(now())

  @@index([userId])
  @@index([expiresAt])
}

model LoginAttempt {
  id             String    @id @default(uuid())
  user           User?     @relation(fields: [userId], references: [id], onDelete: Cascade)
  userId         String?
  emailLowercase String
  ipAddress      String
  failCount      Int       @default(0)
  lockedUntil    DateTime?
  firstFailedAt  DateTime  @default(now())
  lastFailedAt   DateTime  @default(now())
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt

  @@unique([emailLowercase, ipAddress])
  @@index([lockedUntil])
}

enum Role {
  USER
  ADMIN
}

enum OrgMemberRole {
  OWNER
  MEMBER
}

model Organization {
  id                 String              @id @default(cuid())
  name               String
  slug               String              @unique
  members            OrgMember[]
  roles              OrgRole[]
  customers          Customer[]
  serviceLocations   ServiceLocation[]
  technicians        Technician[]
  serviceResources   ServiceResource[]
  skills             Skill[]
  resourceSkills     ResourceSkill[]
  crews              Crew[]
  crewMembers        CrewMember[]
  workOrders         WorkOrder[]
  workTemplates      WorkTemplate[]
  templateTasks      TemplateTask[]
  templateSkillRequirements TemplateSkillRequirement[]
  workOrderIncidents WorkOrderIncident[]
  workOrderTasks     WorkOrderTask[]
  workNotes          WorkNote[]
  workOrderLineItems WorkOrderLineItem[]
  serviceContracts   ServiceContract[]
  contractItems      ContractItem[]
  recurrenceRules    RecurrenceRule[]
  generatedOccurrences GeneratedOccurrence[]
  bookingStatuses    BookingStatus[]
  bookings           Booking[]
  bookingStatusEvents BookingStatusEvent[]
  resourceRequirements ResourceRequirement[]
  routes             Route[]
  routeStops         RouteStop[]
  invoices           Invoice[]
  invoiceWorkOrders  InvoiceWorkOrder[]
  invoiceLines       InvoiceLine[]
  createdAt          DateTime            @default(now())
  updatedAt          DateTime            @updatedAt
}

model OrgMember {
  id        String         @id @default(cuid())
  org       Organization   @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId     String
  user      User           @relation(fields: [userId], references: [id], onDelete: Cascade)
  userId    String
  role      OrgMemberRole  @default(MEMBER)
  roles     MemberRole[]
  serviceResources ServiceResource[]
  bookingStatusEvents BookingStatusEvent[]
  createdAt DateTime       @default(now())
  updatedAt DateTime       @updatedAt

  @@unique([orgId, userId])
  @@index([orgId])
  @@index([userId])
}

model Permission {
  id              String           @id @default(cuid())
  key             String           @unique
  description     String?
  rolePermissions RolePermission[]
  createdAt       DateTime         @default(now())
  updatedAt       DateTime         @updatedAt
}

model OrgRole {
  id              String           @id @default(cuid())
  org             Organization     @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId           String
  name            String
  key             String?
  isSystem        Boolean          @default(false)
  rolePermissions RolePermission[]
  memberRoles     MemberRole[]
  createdAt       DateTime         @default(now())
  updatedAt       DateTime         @updatedAt

  @@unique([orgId, name])
  @@unique([orgId, key])
  @@index([orgId])
}

model RolePermission {
  id           String      @id @default(cuid())
  role         OrgRole     @relation(fields: [roleId], references: [id], onDelete: Cascade)
  roleId       String
  permission   Permission  @relation(fields: [permissionId], references: [id], onDelete: Cascade)
  permissionId String
  createdAt    DateTime    @default(now())

  @@unique([roleId, permissionId])
  @@index([permissionId])
}

model MemberRole {
  id        String     @id @default(cuid())
  member    OrgMember  @relation(fields: [memberId], references: [id], onDelete: Cascade)
  memberId  String
  role      OrgRole    @relation(fields: [roleId], references: [id], onDelete: Cascade)
  roleId    String
  createdAt DateTime   @default(now())

  @@unique([memberId, roleId])
  @@index([roleId])
}

// FSM models
enum WorkOrderStatus {
  DRAFT
  SCHEDULED
  IN_PROGRESS
  COMPLETED
  CANCELLED
}

enum WorkOrderPriority {
  LOW
  NORMAL
  HIGH
  URGENT
}

enum TaskStatus {
  TODO
  DONE
  SKIPPED
}

enum InvoiceStatus {
  DRAFT
  ISSUED
  PAID
  VOID
}

model Customer {
  id                  String            @id @default(cuid())
  org                 Organization?     @relation(fields: [orgId], references: [id], onDelete: SetNull)
  orgId               String?
  name                String
  email               String?
  phone               String?
  billingAddressLine1 String?
  billingAddressLine2 String?
  billingCity         String?
  billingProvince     String?
  billingPostalCode   String?
  billingCountry      String?
  serviceLocations    ServiceLocation[]
  workOrders          WorkOrder[]
  serviceContracts    ServiceContract[]
  invoices            Invoice[]
  createdAt           DateTime          @default(now())
  updatedAt           DateTime          @updatedAt

  @@index([orgId])
}

model ServiceLocation {
  id           String      @id @default(cuid())
  org          Organization? @relation(fields: [orgId], references: [id], onDelete: SetNull)
  orgId        String?
  customer     Customer    @relation(fields: [customerId], references: [id], onDelete: Cascade)
  customerId   String
  label        String?
  addressLine1 String
  addressLine2 String?
  city         String
  province     String?
  postalCode   String?
  country      String?
  latitude     Float?
  longitude    Float?
  workOrders   WorkOrder[]
  serviceContracts ServiceContract[]
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt

  @@index([customerId])
  @@index([orgId])
}

model Technician {
  id          String      @id @default(cuid())
  org         Organization? @relation(fields: [orgId], references: [id], onDelete: SetNull)
  orgId       String?
  user        User        @relation(fields: [userId], references: [id], onDelete: Cascade)
  userId      String      @unique
  displayName String
  phone       String?
  workOrders  WorkOrder[]
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt

  @@index([orgId])
}

model ServiceResource {
  id                 String      @id @default(cuid())
  org                Organization @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId              String
  orgMember          OrgMember?  @relation(fields: [orgMemberId], references: [id], onDelete: SetNull)
  orgMemberId        String?
  legacyTechnicianId String?
  displayName        String
  email              String?
  phone              String?
  isActive           Boolean     @default(true)
  skills             ResourceSkill[]
  crewMemberships    CrewMember[]
  createdAt          DateTime    @default(now())
  updatedAt          DateTime    @updatedAt

  @@unique([orgId, orgMemberId])
  @@unique([orgId, legacyTechnicianId])
  @@index([orgId])
}

model Skill {
  id          String      @id @default(cuid())
  org         Organization @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId       String
  name        String
  description String?
  resources   ResourceSkill[]
  templateSkillRequirements TemplateSkillRequirement[]
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt

  @@unique([orgId, name])
  @@index([orgId])
}

model ResourceSkill {
  id         String          @id @default(cuid())
  org        Organization     @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId      String
  resource   ServiceResource @relation(fields: [resourceId], references: [id], onDelete: Cascade)
  resourceId String
  skill      Skill           @relation(fields: [skillId], references: [id], onDelete: Cascade)
  skillId    String
  createdAt  DateTime        @default(now())

  @@unique([orgId, resourceId, skillId])
  @@index([orgId])
  @@index([resourceId])
  @@index([skillId])
}

model Crew {
  id          String        @id @default(cuid())
  org         Organization  @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId       String
  name        String
  description String?
  members     CrewMember[]
  bookings    Booking[]
  routes      Route[]
  createdAt   DateTime      @default(now())
  updatedAt   DateTime      @updatedAt

  @@unique([orgId, name])
  @@index([orgId])
}

model CrewMember {
  id         String          @id @default(cuid())
  org        Organization     @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId      String
  crew       Crew            @relation(fields: [crewId], references: [id], onDelete: Cascade)
  crewId     String
  resource   ServiceResource @relation(fields: [resourceId], references: [id], onDelete: Cascade)
  resourceId String
  createdAt  DateTime        @default(now())

  @@unique([orgId, crewId, resourceId])
  @@index([orgId])
  @@index([crewId])
  @@index([resourceId])
}

model WorkOrder {
  id                   String              @id @default(cuid())
  org                  Organization?       @relation(fields: [orgId], references: [id], onDelete: SetNull)
  orgId                String?
  customer             Customer            @relation(fields: [customerId], references: [id], onDelete: Cascade)
  customerId           String
  serviceLocation      ServiceLocation     @relation(fields: [serviceLocationId], references: [id], onDelete: Cascade)
  serviceLocationId    String
  assignedTechnician   Technician?         @relation(fields: [assignedTechnicianId], references: [id], onDelete: SetNull)
  assignedTechnicianId String?
  status               WorkOrderStatus     @default(DRAFT)
  priority             WorkOrderPriority   @default(NORMAL)
  summary              String
  description          String?
  scheduledStart       DateTime?
  scheduledEnd         DateTime?
  actualStart          DateTime?
  actualEnd            DateTime?
  incidents            WorkOrderIncident[]
  notes                WorkNote[]
  lineItems            WorkOrderLineItem[]
  bookings             Booking[]
  createdAt            DateTime            @default(now())
  updatedAt            DateTime            @updatedAt
  invoiceLinks         InvoiceWorkOrder[]
  @@index([customerId])
  @@index([serviceLocationId])
  @@index([assignedTechnicianId])
  @@index([orgId])
}

model WorkTemplate {
  id          String   @id @default(cuid())
  org         Organization @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId       String
  name        String
  description String?
  isActive    Boolean  @default(true)
  tasks       TemplateTask[]
  skillRequirements TemplateSkillRequirement[]
  incidents   WorkOrderIncident[]
  contractItems ContractItem[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@unique([orgId, name])
  @@index([orgId])
}

model TemplateTask {
  id          String   @id @default(cuid())
  org         Organization @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId       String
  template    WorkTemplate @relation(fields: [templateId], references: [id], onDelete: Cascade)
  templateId  String
  title       String
  description String?
  sortOrder   Int      @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([orgId])
  @@index([templateId])
}

model TemplateSkillRequirement {
  id          String   @id @default(cuid())
  org         Organization @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId       String
  template    WorkTemplate @relation(fields: [templateId], references: [id], onDelete: Cascade)
  templateId  String
  skill       Skill    @relation(fields: [skillId], references: [id], onDelete: Restrict)
  skillId     String
  quantity    Int      @default(1)
  notes       String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@unique([orgId, templateId, skillId])
  @@index([orgId])
  @@index([templateId])
  @@index([skillId])
}

model WorkOrderIncident {
  id          String   @id @default(cuid())
  org         Organization? @relation(fields: [orgId], references: [id], onDelete: SetNull)
  orgId       String?
  workOrder   WorkOrder @relation(fields: [workOrderId], references: [id], onDelete: Cascade)
  workOrderId String
  template    WorkTemplate? @relation(fields: [templateId], references: [id], onDelete: SetNull)
  templateId  String?
  title       String
  description String?
  sortOrder   Int      @default(0)
  tasks       WorkOrderTask[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([orgId])
  @@index([workOrderId])
  @@index([templateId])
}

model WorkOrderTask {
  id          String   @id @default(cuid())
  org         Organization? @relation(fields: [orgId], references: [id], onDelete: SetNull)
  orgId       String?
  incident    WorkOrderIncident @relation(fields: [incidentId], references: [id], onDelete: Cascade)
  incidentId  String
  title       String
  description String?
  status      TaskStatus @default(TODO)
  sortOrder   Int      @default(0)
  completedAt DateTime?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([orgId])
  @@index([incidentId])
  @@index([status])
}

model BookingStatus {
  id          String              @id @default(cuid())
  org         Organization        @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId       String
  name        String
  description String?
  isDefault   Boolean             @default(false)
  isActive    Boolean             @default(true)
  sortOrder   Int                 @default(0)
  bookings    Booking[]
  events      BookingStatusEvent[]
  createdAt   DateTime            @default(now())
  updatedAt   DateTime            @updatedAt

  @@unique([orgId, name])
  @@index([orgId])
}

model Booking {
  id             String              @id @default(cuid())
  org            Organization        @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId          String
  workOrder      WorkOrder?          @relation(fields: [workOrderId], references: [id], onDelete: SetNull)
  workOrderId    String?
  crew           Crew                @relation(fields: [crewId], references: [id], onDelete: Restrict)
  crewId         String
  status         BookingStatus       @relation(fields: [statusId], references: [id], onDelete: Restrict)
  statusId       String
  scheduledStart DateTime?
  scheduledEnd   DateTime?
  requirements   ResourceRequirement[]
  statusEvents   BookingStatusEvent[]
  routeStops     RouteStop[]
  createdAt      DateTime            @default(now())
  updatedAt      DateTime            @updatedAt

  @@unique([orgId, workOrderId])
  @@index([orgId])
  @@index([workOrderId])
  @@index([crewId])
  @@index([statusId])
}

model ResourceRequirement {
  id           String       @id @default(cuid())
  org          Organization @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId        String
  booking      Booking      @relation(fields: [bookingId], references: [id], onDelete: Cascade)
  bookingId    String
  resourceType String
  quantity     Int          @default(1)
  notes        String?
  createdAt    DateTime     @default(now())
  updatedAt    DateTime     @updatedAt

  @@index([orgId])
  @@index([bookingId])
  @@unique([orgId, bookingId, resourceType])
}

model BookingStatusEvent {
  id           String         @id @default(cuid())
  org          Organization   @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId        String
  booking      Booking        @relation(fields: [bookingId], references: [id], onDelete: Cascade)
  bookingId    String
  status       BookingStatus  @relation(fields: [statusId], references: [id], onDelete: Restrict)
  statusId     String
  actor        OrgMember?     @relation(fields: [orgMemberId], references: [id], onDelete: SetNull)
  orgMemberId  String?
  createdAt    DateTime       @default(now())

  @@index([orgId])
  @@index([bookingId])
  @@index([statusId])
  @@index([orgMemberId])
}

model Route {
  id        String       @id @default(cuid())
  org       Organization @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId     String
  crew      Crew         @relation(fields: [crewId], references: [id], onDelete: Restrict)
  crewId    String
  routeDate DateTime
  stops     RouteStop[]
  createdAt DateTime     @default(now())
  updatedAt DateTime     @updatedAt

  @@unique([orgId, crewId, routeDate])
  @@index([orgId])
  @@index([crewId])
}

model RouteStop {
  id        String       @id @default(cuid())
  org       Organization @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId     String
  route     Route        @relation(fields: [routeId], references: [id], onDelete: Cascade)
  routeId   String
  booking   Booking      @relation(fields: [bookingId], references: [id], onDelete: Cascade)
  bookingId String
  position  Int
  createdAt DateTime     @default(now())
  updatedAt DateTime     @updatedAt

  @@unique([orgId, routeId, position])
  @@unique([orgId, bookingId])
  @@index([orgId])
  @@index([routeId])
  @@index([bookingId])
}

model WorkNote {
  id           String    @id @default(cuid())
  org          Organization? @relation(fields: [orgId], references: [id], onDelete: SetNull)
  orgId        String?
  workOrder    WorkOrder @relation(fields: [workOrderId], references: [id], onDelete: Cascade)
  workOrderId  String
  author       User      @relation(fields: [authorUserId], references: [id], onDelete: Cascade)
  authorUserId String
  body         String
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt

  @@index([workOrderId])
  @@index([authorUserId])
  @@index([orgId])
}

model WorkOrderLineItem {
  id             String    @id @default(cuid())
  org            Organization? @relation(fields: [orgId], references: [id], onDelete: SetNull)
  orgId          String?
  workOrder      WorkOrder @relation(fields: [workOrderId], references: [id], onDelete: Cascade)
  workOrderId    String
  description    String
  quantity       Int       @default(1)
  unitPriceCents Int
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt

  @@index([workOrderId])
  @@index([orgId])
}

model Invoice {
  id         String        @id @default(cuid())
  org        Organization  @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId      String
  customer   Customer      @relation(fields: [customerId], references: [id], onDelete: Cascade)
  customerId String
  status     InvoiceStatus @default(DRAFT)
  dueDate    DateTime?
  memo       String?
  issuedAt   DateTime?
  paidAt     DateTime?
  voidedAt   DateTime?
  workOrders InvoiceWorkOrder[]
  lines      InvoiceLine[]
  createdAt  DateTime      @default(now())
  updatedAt  DateTime      @updatedAt

  @@index([orgId])
  @@index([customerId])
  @@index([status])
}

model InvoiceWorkOrder {
  id          String       @id @default(cuid())
  org         Organization @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId       String
  invoice     Invoice      @relation(fields: [invoiceId], references: [id], onDelete: Cascade)
  invoiceId   String
  workOrder   WorkOrder    @relation(fields: [workOrderId], references: [id], onDelete: Cascade)
  workOrderId String
  createdAt   DateTime     @default(now())

  @@unique([orgId, invoiceId, workOrderId])
  @@index([orgId])
  @@index([invoiceId])
  @@index([workOrderId])
}

model InvoiceLine {
  id             String       @id @default(cuid())
  org            Organization @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId          String
  invoice        Invoice      @relation(fields: [invoiceId], references: [id], onDelete: Cascade)
  invoiceId      String
  description    String
  quantity       Int          @default(1)
  unitPriceCents Int?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt

  @@index([orgId])
  @@index([invoiceId])
}

model RecurrenceRule {
  id           String        @id @default(cuid())
  org          Organization  @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId        String
  rrule        String
  dtstartLocal String
  timeZone     String
  untilLocal   String?
  contracts    ServiceContract[]
  createdAt    DateTime      @default(now())
  updatedAt    DateTime      @updatedAt

  @@index([orgId])
}

model ServiceContract {
  id                String             @id @default(cuid())
  org               Organization       @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId             String
  customer          Customer           @relation(fields: [customerId], references: [id], onDelete: Cascade)
  customerId        String
  serviceLocation   ServiceLocation?   @relation(fields: [serviceLocationId], references: [id], onDelete: SetNull)
  serviceLocationId String?
  recurrenceRule    RecurrenceRule     @relation(fields: [recurrenceRuleId], references: [id], onDelete: Restrict)
  recurrenceRuleId  String
  name              String
  description       String?
  isActive          Boolean            @default(true)
  items             ContractItem[]
  occurrences       GeneratedOccurrence[]
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt

  @@index([orgId])
  @@index([customerId])
  @@index([serviceLocationId])
  @@index([recurrenceRuleId])
}

model ContractItem {
  id             String          @id @default(cuid())
  org            Organization    @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId          String
  contract       ServiceContract @relation(fields: [contractId], references: [id], onDelete: Cascade)
  contractId     String
  workTemplate   WorkTemplate?   @relation(fields: [workTemplateId], references: [id], onDelete: SetNull)
  workTemplateId String?
  title          String
  description    String?
  quantity       Int             @default(1)
  unitPriceCents Int?
  createdAt      DateTime        @default(now())
  updatedAt      DateTime        @updatedAt

  @@index([orgId])
  @@index([contractId])
  @@index([workTemplateId])
}

model GeneratedOccurrence {
  id         String          @id @default(cuid())
  org        Organization    @relation(fields: [orgId], references: [id], onDelete: Cascade)
  orgId      String
  contract   ServiceContract @relation(fields: [contractId], references: [id], onDelete: Cascade)
  contractId String
  startsAt   DateTime
  createdAt  DateTime        @default(now())

  @@unique([orgId, contractId, startsAt])
  @@index([orgId])
  @@index([contractId])
  @@index([startsAt])
}
